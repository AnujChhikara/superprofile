node_modules
dist
# Local env files (secrets) — keep .env.example tracked
.env
.env.local
.env.*.local
!.env.example
deploy-out
*.zip
.superpowers/
# Built widget artifacts (generated into server/public by `pnpm --filter widget build`)
server/public/widget/
server/public/widget.js
